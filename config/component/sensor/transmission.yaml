- platform: transmission
  host: !secret transmission_host
  port: !secret transmission_port
  monitored_variables:
    - 'current_status'
    - 'download_speed'
    - 'upload_speed'
    - 'active_torrents'
